spring:
  application:
    name: portfolio-api
  datasource:
    url: jdbc:postgresql://localhost:5432/portfolio
    username: User
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

jwt:
  secret: this-is-a-demo-secret-key-please-change-it-at-least-32-bytes
  expiration: 86400000    # 24시간